dari "Konsol.idsm" impor { publik println };

fungsi utama(): Angka {
    var nilai: Angka = 7;
    var *ptr: Angka = &nilai;
    
    println("Nilai awal: " + Teks(nilai));
    ptr = 10;
    println("Nilai setelah pointer: " + Teks(nilai));
    kembalikan 0;
}
